font_awesome_token: <%= ENV['FONT_AWESOME_TOKEN'] %>
development:
  secret_key_base: <%= SecureRandom.hex(64) %>
  secret_key_turnstile: <%= SecureRandom.hex(32) %>
test:
  secret_key_base: <%= SecureRandom.hex(64) %>
  secret_key_turnstile: <%= SecureRandom.hex(32) %>
