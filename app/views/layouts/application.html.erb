<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= render partial: 'web_analytics' if CONFIG[:web_analytics] %>
    <title><%= [@finding_aid_title, @repository_name, 'Columbia University Libraries Finding Aids'].reject(&:blank?).join(' | ') %></title>
    <%= csp_meta_tag %>
    <%= csrf_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_typescript_tag 'application.tsx' %>
    <%= vite_stylesheet_tag 'application.scss' %>
    <link rel="stylesheet" href="https://vjs.zencdn.net/7.11.4/video-js.css"/>
  </head>

  <body class="<%= render_body_class %>">
    <%= render Acfa::HeaderComponent.new(blacklight_config: controller.blacklight_config) unless @print_view %>
    <div class="container">
      <%= render partial: 'shared/flash_msg', layout: 'shared/flash_messages' %>
      <%= yield %>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>
